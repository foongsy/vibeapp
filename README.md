# min-monorepo

Minimal monorepo template for a React TypeScript frontend and Python FastAPI backend web application using Cursor.

> 💥 NOTE
> This is 99% generated by Cursor for beginner to play with monorepo with backend + frontend while vibecoding.
> It is not meant to be for complex production service and use at your own risk.

## Overview

This starter includes:
- **Backend**: Pre-configured FastAPI app (Python 3.13+)
- **Frontend**: Created on-demand via Cursor command (React + TypeScript + Vite)

The frontend is generated when you run the Cursor command, so you always get the latest Vite/React versions.

## Quick Start

### 1) Backend
```bash
cd backend
uv sync
uv run main.py
```
Backend runs on `http://localhost:8000`.

### 2) Frontend (initialize via Cursor)
- Run Cursor command `@init-frontend` from the repo root.
  - Detects package manager (prefers `bun`, falls back to `npm`)
  - Scaffolds Vite + React + TypeScript
  - Configures `/api` proxy to the backend
  - Adds example API integration

After it creates `frontend/`:
```bash
cd frontend
npm install  # or bun install
npm run dev  # or bun run dev
```
Frontend runs on `http://localhost:5173`.

## Project Structure

```
min-monorepo/
├── backend/              # FastAPI application (included)
│   ├── app/
│   │   ├── routes/       # API route modules
│   │   ├── config.py     # Pydantic settings
│   │   └── cors.py       # CORS configuration
│   ├── main.py           # FastAPI entry point
│   └── pyproject.toml
│
├── frontend/             # React app (created via @init-frontend)
│   ├── src/
│   │   └── App.tsx       # Example API integration
│   └── vite.config.ts    # API proxy config
│
└── .cursor/
    └── commands/
        └── init-frontend.mdc  # Frontend initialization command
```

## Features

### Backend (included)
- FastAPI with automatic OpenAPI docs at `/openapi.json`
- Pydantic settings, CORS pre-configured
- Example endpoints: `GET /api/hello`, `GET /health`
- Environment-based config via `.env`

### Frontend (created via command)
- React 19+ with TypeScript
- Vite dev/build, ESLint configured
- `/api` proxy to `http://localhost:8000`
- Example fetch to `/api/hello` shown in `App.tsx`

## Development Workflow
1) Start backend: `cd backend && uv run main.py`
2) Initialize frontend (first time only): run `@init-frontend`
3) Start frontend: `cd frontend && npm run dev` (or `bun run dev`)

## API Endpoints
- `GET /api/hello` → `{ "message": "Hello from FastAPI!" }`
- `GET /health` → `{ "status": "ok" }`
- `GET /openapi.json` → OpenAPI schema

## Tech Stack
- Backend: Python 3.13+, FastAPI, Uvicorn, Pydantic Settings, package manager `uv`
- Frontend (after init): React 19+, TypeScript 5.9+, Vite 7+, package manager `bun` (preferred) or `npm`

## Prerequisites
- Python 3.13+ with `uv` installed
- Node.js with npm (or bun for faster installs)
- Cursor (for `@init-frontend`)

## Notes
- Backend uses `uv` (not pip)
- Frontend is generated fresh to stay up to date
- CORS allows `http://localhost:5173`
- The `@init-frontend` command wires the proxy and sample API call for you

---

## 🎯 Linear Issue AHS-82: Roadmap.sh Analysis

**Issue**: Analyze roadmap.sh to find the easiest way to extract role-based roadmap information

**Status**: ✅ COMPLETE

### Quick Access

- **Start Here**: [`INDEX.md`](INDEX.md) - Complete navigation and overview
- **Executive Summary**: [`LINEAR_ISSUE_AHS-82_SUMMARY.md`](LINEAR_ISSUE_AHS-82_SUMMARY.md)
- **Visual Guide**: [`VISUAL_SUMMARY.md`](VISUAL_SUMMARY.md)
- **Quick Test**: Run `python quick_example.py`

### Answer

**The easiest way**: Direct HTTP access to GitHub repository raw files

- ✅ No authentication required
- ✅ 71 roadmaps available (backend, frontend, devops, etc.)
- ✅ Simple HTTP GET requests
- ✅ Working Python implementation provided
- ✅ Sample data extracted and verified

### Deliverables

```
📄 Documentation:   6 files (2,055 lines of comprehensive docs)
💻 Implementation:  3 files (working Python scripts)
📊 Sample Data:     5 files (backend, frontend, devops, full-stack roadmaps)
```

### Quick Start

```bash
# Install dependencies
pip install -r requirements.txt

# Run minimal example (extracts backend roadmap)
python quick_example.py

# Or run full extractor (extracts 4 popular roadmaps)
python roadmap_extractor.py
```

See [`INDEX.md`](INDEX.md) for complete documentation and navigation.
